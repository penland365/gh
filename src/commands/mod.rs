pub mod orgs;

use rustc_serialize::json::as_pretty_json;

//fn print_pretty_jsonncoderUnstable>(x: T) -> () {
fn print_pretty_json() -> () {
    //Som(format) => if format == "json" {
    //    let json = json::as_pretty_json(&decoded);
    //    println!("{}", json);
    //} else {
    //    panic!("unknown format request {}", format);
    //}
}
